// Simple example that works with our current implementation
Auto name = "Rubber Duck";
Auto version = 1.0;
Auto is_cool = true;

Display("Language: " + name);
Display("Version: " + version);
Display("Is cool: " + is_cool);

// Expressions
Auto a = 10;
Auto b = 20;
Auto sum = a + b;
Auto product = a * b;

Display("Sum: " + sum);
Display("Product: " + product);

// Grouping
Auto result = (a + b) * 2;
Display("Result: " + result);